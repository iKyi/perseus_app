export type ProjectsPagePropsType = {
  children?: any;
};

const ProjectsPage: React.FC<ProjectsPagePropsType> = ({ children }) => {
  // *************** RENDER *************** //
  return <></>;
};

export default ProjectsPage;
